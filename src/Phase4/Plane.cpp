#include "Plane.h"

/*
Plane::Plane(Vector3 newNormale) {
	normale = newNormale;
	offset = 0.0;
}

Plane::Plane(Vector3 newNormale, float newOffset) {
	normale = newNormale;
	offset = newOffset;
};

Vector3 Plane::getNormale() {
	return normale;
};

void Plane::setNormale(Vector3 newNormale) {
	normale = newNormale;
};

float Plane::getOffset() {
	return offset;
};

void Plane::setOffset(float newOffset) {
	offset = newOffset;
};

*/